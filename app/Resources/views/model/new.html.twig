{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-4">
        <h1>Create Model</h1>
        <div class="form-panel">
            <a href="{{ path('model_index') }}" class="btn btn-default">
                <i class="glyphicon glyphicon-arrow-left"></i>
            </a>
        </div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Save</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
